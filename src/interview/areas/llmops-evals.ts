import type { Area } from "../types";

export const llmopsEvals: Area = {
  slug: "llmops-evals",
  title: "LLM Evals, Guardrails & Ops",
  icon: "🛡️",
  stream: "AI Engineering",
  blurb:
    "How you know a change helped, how you stop the model doing something harmful, and how you run all of it in production without surprises.",

  mustKnow: [
    "You cannot improve what you don't measure. An eval set is the single highest-value artefact in an LLM project — build it before optimising prompts.",
    "Eyeballing a few outputs is not evaluation; it's how regressions ship. Prompt changes routinely improve one case and break three others.",
    "Evaluate per category and per slice, not as one aggregate number — an average hides a regression in the cases that matter.",
    "LLM-as-judge scales evaluation but must itself be validated against human labels before you trust it.",
    "Track cost and latency alongside quality; an improvement that triples the bill is a trade, not a win.",
    "Guardrails belong at both ends: validate input (injection, PII, abuse) and output (schema, groundedness, policy, PII leakage).",
    "Non-determinism means you should run evals multiple times per case and report a rate with variance, not a single pass/fail.",
    "Log every production request — prompt, model, parameters, output, latency, cost, and a trace id — or you cannot debug or build the next eval set.",
    "Pin model versions. A silently-updated model is an unannounced deploy of your most important dependency.",
    "Ship behind a flag with a rollback path, and treat prompts as versioned artefacts reviewed like code.",
  ],

  sheets: [
    {
      title: "Building an eval set",
      points: [
        "Source from real traffic, not imagination — production queries reflect the distribution you actually face.",
        "Include the failures: every bug report and complaint becomes a permanent test case.",
        "Cover slices explicitly: query types, languages, edge cases, long inputs, adversarial inputs, and 'should refuse' cases.",
        "Include abstention cases with 'I don't know' as the expected answer, or every optimisation pushes toward answering.",
        "Keep a held-out set you never iterate against — otherwise you overfit the prompt to your eval.",
        "Start small: 30–50 well-chosen cases beat 1,000 generated ones.",
        "Version the eval set alongside the prompt, and re-run in CI on every change.",
      ],
    },
    {
      title: "Choosing a grader",
      points: [
        "Exact match / regex — extraction, classification, routing. Cheap, deterministic, unambiguous.",
        "Schema validation — structured output; is it parseable and does it satisfy the schema?",
        "Programmatic checks — does the generated SQL run, does the code compile, does the test pass? The strongest signal available.",
        "Semantic similarity to a reference — noisy, but useful for summarisation.",
        "LLM-as-judge with an explicit rubric — for open-ended generation. Validate against human labels first.",
        "Pairwise comparison ('which is better, A or B?') is more reliable from a judge than absolute scoring.",
        "Human review on a sample — the ground truth everything else is calibrated against.",
      ],
    },
    {
      title: "LLM-as-judge: making it trustworthy",
      points: [
        "Give a specific rubric with criteria and examples, not 'rate 1–10'.",
        "Ask for a short justification before the score — it improves consistency.",
        "Prefer pairwise comparison or a small discrete scale over fine-grained numeric scores.",
        "Randomise the order of candidates — judges have a position bias.",
        "Known biases: verbosity (longer looks better), self-preference (a model prefers its own style), position, and formatting.",
        "Validate: label 50–100 examples by hand and measure agreement with the judge before trusting it.",
        "Use a different (often stronger) model as judge than the one being judged where you can.",
      ],
    },
    {
      title: "Guardrails",
      points: [
        "Input: prompt-injection detection, PII detection and redaction, abuse/topic classification, length and rate limits.",
        "Output: schema validation, groundedness/citation checking, PII leakage scan, policy classification, refusal handling.",
        "Deterministic checks first — they're cheap, fast, and explainable. Reserve model-based checks for what rules can't catch.",
        "Fail safe: decide up front what happens when a guardrail fires — block, redact, escalate to a human, or degrade to a canned response.",
        "Guardrails add latency; run independent ones in parallel and stream what you can.",
        "Log every trigger — guardrail hit rates are an early warning of both abuse and prompt regressions.",
        "No guardrail is complete; combine them with least privilege so a bypass has bounded consequences.",
      ],
    },
    {
      title: "Production observability for LLM features",
      points: [
        "Log per request: prompt (or a hash if sensitive), model + version, parameters, output, token counts, latency, cost, trace id, user/session.",
        "Metrics: p50/p95/p99 latency, time-to-first-token, error and timeout rate, cost per request, token distribution.",
        "Quality signals: guardrail trigger rate, schema-validation failure rate, retrieval hit rate, abstention rate.",
        "User signals: thumbs up/down, regeneration rate, copy rate, escalation to human — the cheapest proxies for quality.",
        "Sample and review production traffic weekly; feed failures back into the eval set.",
        "Alert on distribution shift in inputs and on cost per request, not only on errors.",
        "Trace multi-step flows end to end — for agents and RAG, one call's log is not enough.",
      ],
    },
  ],

  decisions: [
    {
      title: "Offline evals vs online A/B testing",
      options: [
        {
          name: "Offline eval set",
          pick: ["Every prompt, model, or retrieval change — fast, cheap, no users exposed.", "Regression prevention in CI.", "Comparing candidates before shipping anything."],
          cost: ["Only measures what's in the set; blind to real-world distribution you haven't captured.", "Proxy metrics may not correlate with what users care about."],
        },
        {
          name: "Online A/B",
          pick: ["The decision depends on real user behaviour: engagement, task completion, escalation rate.", "Final validation of a change that passed offline."],
          cost: ["Slow — needs traffic and time for significance.", "Exposes users to a possibly-worse experience.", "Confounded by everything else changing."],
        },
      ],
      verdict: "Offline evals as the gate on every change; online A/B to confirm the ones that matter. Neither replaces the other.",
      topic: "ml-evaluation",
    },
    {
      title: "Rules-based guardrails vs model-based",
      options: [
        {
          name: "Deterministic rules",
          pick: ["Anything expressible precisely: schema validation, regex for PII patterns, blocklists, length caps, rate limits.", "You need speed, zero cost, and an explainable decision."],
          cost: ["Brittle against paraphrase and obfuscation.", "Both false positives and gaps as attackers adapt."],
        },
        {
          name: "Model-based classifier",
          pick: ["Judgement calls: is this abusive, off-topic, an injection attempt, unsupported by the sources?", "Handles paraphrase and novel phrasing."],
          cost: ["Latency and cost per request.", "Itself fallible and itself attackable.", "Needs its own eval set and monitoring."],
        },
      ],
      verdict: "Rules first for everything they can cover, model-based only for the judgement layer — and treat neither as sufficient alone.",
    },
    {
      title: "Pin the model version or track latest?",
      options: [
        {
          name: "Pinned version",
          pick: ["Production. You control when behaviour changes and can test before it does.", "Regulated environments needing reproducibility."],
          cost: ["Manual upgrade work.", "Pinned versions get deprecated on the provider's schedule, so you must track end-of-life dates."],
        },
        {
          name: "Latest alias",
          pick: ["Prototypes and internal tools where a behaviour change is tolerable.", "You want improvements without maintenance."],
          cost: ["An unannounced deploy of your most important dependency — prompts tuned for one version can regress on the next.", "Silent quality, latency, and cost changes."],
        },
      ],
      verdict: "Pin in production, run evals against the new version in CI before upgrading, and keep a rollback path. Track latest only in development.",
    },
  ],

  qa: [
    {
      id: "ops-why-evals",
      q: "How do you know a prompt change is an improvement?",
      a: "By running it against an eval set, because the alternative — trying a few examples by hand — systematically misleads. Prompt changes are not local: adding an instruction to fix one failure routinely degrades cases you weren't looking at, and you won't notice because you only checked the case you were fixing. So you build a dataset of representative inputs with expected outputs or graded criteria, drawn from real traffic and deliberately including known failures, then grade with whatever's appropriate — exact match for extraction, schema validation for structured output, a programmatic check where one exists, or an LLM judge with a rubric for open-ended text. Run it on every change in CI, and report per-category scores rather than an aggregate, since a single number will happily hide a regression in the slice you care most about. Two disciplines make it trustworthy: hold out part of the set so you're not overfitting the prompt to your evaluation, and run each case several times because the model is non-deterministic, reporting a success rate rather than a binary result.",
      why: "Evaluation discipline is the clearest divider between teams that ship LLM features reliably and teams that demo them.",
      trap: "Testing against the same handful of examples used to write the prompt. That measures fit to your examples, not generalisation.",
      followUps: ["How would you build the first eval set?", "Why report per-category scores?"],
      depth: "Core",
      tags: ["evaluation"],
      topic: "ml-evaluation",
    },
    {
      id: "ops-llm-judge",
      q: "When can you trust an LLM as a judge?",
      a: "After you've validated it against human labels, and not before. The appeal is obvious — it scales to thousands of open-ended outputs where humans can't — but it has documented biases you have to control for. It favours longer answers regardless of quality; it prefers text in its own style, so using the same model to judge itself is self-serving; it's sensitive to the order candidates are presented in; and it's swayed by formatting and confident tone. The mitigations are concrete: give an explicit rubric with criteria and worked examples rather than asking for a score out of ten; require a brief justification before the score, which measurably improves consistency; prefer pairwise comparison — 'which of these two is better and why' — since relative judgements are more reliable than absolute ones; randomise presentation order and average over both orders; and use a different, ideally stronger, model as the judge. Then validate: hand-label fifty to a hundred examples, measure agreement, and only proceed if it's high enough on the cases you care about. Re-validate whenever you change the judge model or the rubric.",
      why: "It's now the standard evaluation technique for generative output, and its failure modes are what determine whether your metrics mean anything.",
      trap: "Using the same model to generate and to judge, then reporting the score as objective. Self-preference bias inflates it.",
      followUps: ["Why is pairwise more reliable than absolute scoring?", "How do you measure judge-human agreement?"],
      depth: "Advanced",
      tags: ["evaluation"],
      topic: "ml-evaluation",
    },
    {
      id: "ops-guardrails",
      q: "What guardrails would you put around an LLM feature?",
      a: "Both ends of the call, and layered. On input: length and rate limits so a single user can't exhaust your budget; PII detection and redaction if the prompt is logged or sent to a third party; abuse and topic classification to reject requests outside the feature's scope; and injection detection for anything that will reach a tool-using agent. On output: schema validation so malformed structured output never reaches downstream code; groundedness checking against retrieved sources for RAG, since an unsupported claim is your main hallucination risk; a PII and secret scan so the model doesn't echo back something from context it shouldn't; and a policy classifier for harmful content. Two design points matter more than the list. Order them cheap-to-expensive — deterministic checks first, model-based only for judgement calls — and run independent checks in parallel so you're not stacking latency. And decide the failure behaviour explicitly for each: block, redact, retry with a corrective message, escalate to a human, or fall back to a canned response. Then log every trigger, because the hit rate is an early warning of both abuse and prompt regressions.",
      why: "It's the practical safety question, and knowing that guardrails are layered and fallible is what makes an answer credible.",
      trap: "Relying on system-prompt instructions as a guardrail. Instructions are a preference, not a control — the enforcement has to be outside the model.",
      followUps: ["What should happen when a guardrail fires?", "How do you keep guardrails from doubling your latency?"],
      depth: "Advanced",
      tags: ["safety", "reliability"],
    },
    {
      id: "ops-hallucination-detection",
      q: "How do you detect hallucination automatically?",
      a: "The tractable version is groundedness checking rather than truth checking: in a RAG system you have the retrieved sources, so you can ask whether each claim in the answer is supported by them, which is a well-defined entailment question a model or a specialised classifier can answer reasonably well. Requiring the generator to cite spans makes this much easier, because you can verify that cited ids actually exist in the retrieved set — models do sometimes invent plausible identifiers — and then check that the cited span supports the claim. Where there's no source corpus the options are weaker but still useful: self-consistency, sampling several answers at a non-zero temperature and flagging disagreement, since a hallucinated fact tends to vary while a known one is stable; cross-checking against a structured source like a database or search API where the claim is checkable; and for closed-domain tasks, validating extracted values against the input text. What doesn't work well is asking the model how confident it is, since verbalised confidence is poorly calibrated. In production, combine automated groundedness scoring with sampled human review and a user-reporting path.",
      why: "It's the central reliability problem, and the reframing from 'is it true' to 'is it supported' is what makes it solvable.",
      trap: "Asking the model to rate its own confidence and thresholding on that. It's not calibrated and confident errors are exactly the dangerous case.",
      followUps: ["How does self-consistency sampling work?", "Why verify that cited ids exist?"],
      depth: "Advanced",
      tags: ["reliability", "evaluation"],
    },
    {
      id: "ops-monitoring",
      q: "What do you monitor for an LLM feature in production?",
      a: "The usual service signals plus several specific to this class of system. Operationally: p50/p95/p99 latency and time-to-first-token separately, since the first byte drives perceived speed; error, timeout, and rate-limit rates from the provider; and cost per request with token distributions, because cost is driven by a long tail of large prompts you won't see in the average. Quality proxies you can compute without labels: schema-validation failure rate, guardrail trigger rate, retrieval hit rate and score distribution for RAG, abstention rate, and output length distribution — a sudden shift in any of these usually means something changed upstream. User signals are the cheapest real quality measure: thumbs up and down, regeneration rate, copy rate, and escalation to a human. Structurally, log every request with a trace id and trace multi-step flows end to end, because for RAG and agents a single call's log tells you nothing about where a failure originated. Then sample production traffic weekly for human review and feed the failures back into the eval set, which is what keeps evaluation aligned with reality.",
      why: "It shows the candidate treats an LLM feature as a production system rather than a prompt.",
      trap: "Monitoring only errors and latency. The characteristic LLM failure is a confident, well-formed, wrong answer, which is a 200 OK.",
      followUps: ["Which signals work without ground-truth labels?", "Why alert on cost per request?"],
      depth: "Advanced",
      tags: ["observability"],
      topic: "metrics",
    },
    {
      id: "ops-regression",
      q: "The provider updated the model and quality dropped. How do you handle it?",
      a: "Prevent it first: pin the model version in production rather than pointing at a moving alias, because an alias means the provider deploys to your users on their schedule. With pinning, an upgrade becomes a change you control — you run your eval suite against the new version in CI, compare per-category scores plus cost and latency, and only promote when it passes. Prompts tuned against one version genuinely can regress on the next, so the eval run is not a formality. If a regression has already happened, roll back to the pinned version if one is still available, and if the old version is being retired you need a plan: quantify the regression per slice with your eval set so you know what specifically broke, then re-tune the prompt or few-shot examples against the new version, since a change that hurt one behaviour often has a straightforward prompt fix. Track provider deprecation notices so end-of-life dates don't surprise you, and keep an abstraction over the provider so switching models is a config change rather than a refactor — that's what makes the fallback real rather than theoretical.",
      why: "Model dependency management is a genuinely new operational concern and few candidates have thought it through.",
      trap: "Using a `latest` alias in production. That's an unannounced deploy of your most consequential dependency.",
      followUps: ["What goes in your model-upgrade checklist?", "How do you keep switching providers cheap?"],
      depth: "Advanced",
      tags: ["operations"],
    },
    {
      id: "ops-prompt-versioning",
      q: "How should prompts be managed in a codebase?",
      a: "As versioned artefacts with the same rigour as code, because a prompt is production logic. Keep them in version control so changes are diffable and reviewable, and require the eval suite to pass in CI on any change — a prompt edit is a behaviour change and deserves the same gate a code change gets. Keep them in separate files or a registry rather than inlined as string literals scattered through the codebase, so they're findable and so non-engineers can read them. Reference a prompt version in your request logs so that when you investigate an output weeks later you know exactly what was sent. Template variables should be explicit and validated, since a missing interpolation produces a subtly broken prompt rather than an error. Some teams reach for a prompt-management SaaS to allow editing without deploys; that's a real convenience for fast iteration but it separates prompt changes from code review and from your deployment history, so if you do it, keep the eval gate and the audit trail. The anti-pattern is prompts as inline strings edited directly in production with no review and no test.",
      why: "It reveals whether someone treats LLM features as engineering or as configuration tweaking.",
      trap: "Inlining prompts as string literals throughout the code. They become unfindable, unreviewable, and impossible to diff meaningfully.",
      followUps: ["Should prompts be editable without a deploy?", "What do you log to reproduce a past output?"],
      depth: "Intermediate",
      tags: ["engineering"],
    },
    {
      id: "ops-cost-monitoring",
      q: "How do you keep LLM costs predictable?",
      a: "Instrument first: log token counts in and out per request, tagged by feature, endpoint, and user, because cost problems are almost always concentrated in a few call sites or a long tail of unusually large prompts that the average conceals. Then control the inputs to cost. Cap input size and truncate or summarise long conversation histories rather than resending everything, since a chat that grows unbounded grows cost quadratically. Cap output with `max_tokens` and prefer structured output over prose, which often halves generation length. Use prompt caching for stable prefixes — system prompts, few-shot examples, large documents — which is the single biggest lever for chat and agent workloads because those prefixes are re-sent on every turn. Route by difficulty, sending classification and extraction to a small model and reserving the large one for hard reasoning. Cache complete responses for repeated identical queries, and use batch APIs for anything non-interactive. Then add guardrails on spend: per-user and per-tenant rate limits, a budget check before expensive calls, and alerting on cost per request rather than only total spend, so a change in behaviour is visible before the invoice.",
      why: "Unpredictable cost is a common reason LLM features get pulled, and per-request alerting is the non-obvious part.",
      trap: "Monitoring only total monthly spend. By the time that moves you've been paying for a regression for weeks.",
      followUps: ["Why does prompt caching help agents most?", "How would you set a per-tenant budget?"],
      depth: "Advanced",
      tags: ["cost"],
    },
    {
      id: "ops-red-team",
      q: "How would you red-team an LLM feature before launch?",
      a: "Systematically attack it the way a motivated user would, and turn every success into a permanent test case. Cover several categories rather than improvising: prompt injection, both direct and indirect through retrieved documents or tool output; jailbreaks attempting to elicit content the system should refuse, including role-play framings and encoded instructions; data exfiltration, trying to get the model to reveal its system prompt, other users' data, or content from context it shouldn't surface; PII handling, checking whether personal data in inputs ends up in logs or in responses; and misuse specific to your domain, which is where the real risk usually is — for a financial assistant, that's whether it can be led into giving specific investment advice. Run it with people who didn't build the feature, since authors unconsciously avoid the paths they know are weak, and supplement manual work with automated adversarial suites. Every successful attack becomes a permanent eval case so it can't regress. And be explicit that red-teaming reduces risk rather than eliminating it, which is why it pairs with least privilege, human approval on consequential actions, and monitoring for novel attacks in production.",
      why: "Pre-launch safety review is increasingly expected, and the systematic-categories approach beats ad-hoc poking.",
      trap: "Treating red-teaming as a one-off gate. New attacks appear continuously, so it needs to be periodic and paired with production monitoring.",
      followUps: ["What's indirect prompt injection?", "How do you test whether the system prompt can be extracted?"],
      depth: "Advanced",
      tags: ["safety"],
    },
  ],

  pitfalls: [
    "Don't judge a prompt change by trying a few examples — run an eval set.",
    "Don't report one aggregate score — per-slice scores are where regressions hide.",
    "Don't trust an LLM judge without validating it against human labels.",
    "Don't use the same model to generate and judge and call the score objective.",
    "Don't treat system-prompt instructions as guardrails — enforcement must be outside the model.",
    "Don't ask the model for its confidence and threshold on it — it isn't calibrated.",
    "Don't point production at a `latest` model alias.",
    "Don't monitor only errors and latency — the characteristic failure is a confident wrong answer with a 200 status.",
  ],

  numbers: [
    { label: "Useful first eval set", value: "30–50 cases", note: "Well-chosen beats large and generated." },
    { label: "Judge validation sample", value: "50–100 human labels", note: "Measure agreement before trusting the judge." },
    { label: "Runs per eval case", value: "3–5", note: "Non-determinism means one run tells you little." },
    { label: "Alert on", value: "Cost per request", note: "Total spend moves too slowly to catch a regression." },
  ],

  relatedTopics: [
    "ml-evaluation",
    "prompting-fundamentals",
    "structured-output",
    "rag-fundamentals",
    "model-serving",
    "metrics",
    "logging",
    "distributed-tracing",
  ],
};
